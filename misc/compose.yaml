services:
  muse:
    container_name: muse
    image: ghcr.io/museofficial/muse:2.11.6
    restart: always
    volumes:
      - /mnt/Starlink/Stacks/homelab-stacks/misc/configs/muse:/data
    environment:
      - DISCORD_TOKEN=${DISCORD_TOKEN}
      - YOUTUBE_API_KEY=${YOUTUBE_API_KEY}
      - SPOTIFY_CLIENT_ID=${SPOTIFY_CLIENT_ID}
      - SPOTIFY_CLIENT_SECRET=${SPOTIFY_CLIENT_SECRET}

  scrutiny:
    container_name: scrutiny
    image: ghcr.io/analogj/scrutiny:v0.9.3-omnibus
    cap_add:
      - SYS_RAWIO
    ports:
      - "9090:8085" # webapp
      - "8086:8086" # influxDB admin
    volumes:
      - /run/udev:/run/udev:ro
      - ./config:/opt/scrutiny/config
      - ./influxdb:/opt/scrutiny/influxdb
    devices:
      - "/dev/sda"
      - "/dev/sdb"
      - "/dev/sdc"
      - "/dev/sdd"
      - "/dev/sde"
      - "/dev/sdf"
      - "/dev/sdg"
      - "/dev/sdh"
      - "/dev/sdi"
      - "/dev/sdj"
      - "/dev/sdk"
      - "/dev/sdm"
  threadfin:
    container_name: threadfin
    image: fyb3roptik/threadfin:1.2.37
    ports:
      - 34400:34400
    environment:
      - PUID=568
      - PGID=568
      - TZ=America/Toronto
    volumes:
      - /mnt/Starlink/Stacks/homelab-stacks/misc/configs/threadfin/conf:/home/threadfin/conf
      - /mnt/Starlink/Stacks/homelab-stacks/misc/configs/threadfin/temp:/tmp/threadfin:rw
    restart: unless-stopped

  teslacam-viewer:
    container_name: teslacam-viewer
    image: ghcr.io/adsouza98/teslacam-viewer:1.4.2
    restart: unless-stopped
    ports:
      - "${TESLACAM_PORT:-8000}:8000"
    environment:
      - PUID=${PUID:-568}
      - PGID=${PGID:-568}
      - TZ=${TZ:-America/Toronto}
      - AUTH_USER=${TESLACAM_AUTH_USER:-}
      - AUTH_PASS=${TESLACAM_AUTH_PASS:-}
    volumes:
      - ${TESLACAM_MEDIA_PATH:-/mnt/Starlink/Tesla/TeslaUSB}:/media:ro
      - /mnt/Starlink/Stacks/homelab-stacks/misc/configs/teslacam-viewer:/cache
