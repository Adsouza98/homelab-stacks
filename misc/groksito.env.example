# Copy these into the Portainer misc stack environment.
# Do not reuse Muse's DISCORD_TOKEN.
# OAuth tokens are NOT set here — they live on the oauth volume after --login-oauth.

GROKSITO_DISCORD_BOT_TOKEN=
GROKSITO_ALLOWED_GUILD_IDS=
